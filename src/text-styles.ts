import chalk from 'chalk';

const titleChalk = chalk.underline.bold.white;

const savingChalk = chalk.italic.blue;
const loadingChalk = chalk.italic.green;

const okChalk = chalk.bold.green;
const errorChalk = chalk.bold.red;

export
{
    errorChalk,
    loadingChalk,
    okChalk,
    savingChalk,
    titleChalk,
};
